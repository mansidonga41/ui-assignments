import { atom } from "jotai";

export const cartData = atom([]);
export const productsData = atom([]);
export const originalProductsData = atom([]);
export const cartTotalPrice = atom(0);
export const cartQuantity = atom(0);
